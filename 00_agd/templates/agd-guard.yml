name: AGD Guard

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate AGD docs
        run: |
          ./00_agd/agd_en.exe check-all agd/agd_docs --strict
